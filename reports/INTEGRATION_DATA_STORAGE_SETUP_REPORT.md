# 整合模組資料儲存結構建立報告

**日期**: 2025-11-16  
**目的**: 建立標準化的整合模組資料儲存結構,統一管理攻擊路徑、經驗記錄、訓練資料集等

---

## ✅ 已完成項目

### 1. 📂 資料儲存目錄結構

已在 `data/integration/` 建立以下標準化目錄:

```
data/integration/
├── attack_paths/           # 攻擊路徑圖資料
│   ├── attack_graph.pkl    # NetworkX 圖持久化檔案
│   └── exports/            # 匯出的可視化檔案 (HTML, Mermaid)
│
├── experiences/            # 經驗記錄資料庫
│   ├── experience.db       # SQLite 經驗庫 (主資料庫)
│   └── exports/            # 匯出的訓練資料集 (JSONL, CSV)
│
├── training_datasets/      # 訓練資料集
│   ├── dataset_*.jsonl     # 訓練資料集 (JSONL 格式)
│   └── metadata/           # 資料集元資料
│
├── models/                 # 訓練模型檢查點
│   ├── attack_*.pth        # PyTorch 模型檔案
│   └── checkpoints/        # 訓練檢查點
│
├── backups/                # 備份目錄
│   ├── attack_paths/       # 攻擊路徑圖備份
│   ├── experiences/        # 經驗資料庫備份
│   ├── training_datasets/  # 訓練資料集備份
│   └── models/             # 模型檢查點備份
│
└── logs/                   # 日誌目錄
    ├── attack_path_analyzer.log
    ├── experience_repository.log
    └── training.log
```

### 2. 🔧 配置檔案

#### `services/integration/aiva_integration/config.py`
統一配置管理,包含:
- 基礎路徑配置 (INTEGRATION_DATA_DIR)
- 攻擊路徑分析配置 (ATTACK_GRAPH_FILE, ATTACK_PATHS_DIR)
- 經驗資料庫配置 (EXPERIENCE_DB_URL, EXPERIENCES_DIR)
- 訓練資料集配置 (TRAINING_DATASET_DIR)
- 模型檢查點配置 (MODEL_CHECKPOINT_DIR)
- PostgreSQL 配置 (POSTGRES_DSN)
- 備份配置 (BACKUP_DIR, BACKUP_RETENTION_DAYS)

**使用範例**:
```python
from services.integration.aiva_integration.config import (
    ATTACK_GRAPH_FILE,
    EXPERIENCE_DB_URL,
    POSTGRES_DSN
)

# 攻擊路徑引擎
engine = AttackPathEngine(graph_file=ATTACK_GRAPH_FILE)

# 經驗資料庫
repo = ExperienceRepository(database_url=EXPERIENCE_DB_URL)

# 圖建構器
builder = GraphBuilder(engine, POSTGRES_DSN)
```

### 3. 📝 維護腳本

#### `services/integration/scripts/backup.py`
自動備份腳本,支援:
- 備份攻擊路徑圖 (attack_graph.pkl)
- 備份經驗資料庫 (experience.db)
- 備份訓練資料集目錄
- 備份模型檢查點目錄
- 自動清理舊備份 (可配置保留天數)

**用法**:
```bash
# 完整備份
python services/integration/scripts/backup.py

# 僅備份攻擊路徑圖
python services/integration/scripts/backup.py --attack-graph-only

# 僅備份經驗資料庫
python services/integration/scripts/backup.py --experience-only

# 備份但不清理舊備份
python services/integration/scripts/backup.py --no-cleanup
```

#### `services/integration/scripts/cleanup.py`
資料清理腳本,支援:
- 清理舊備份檔案
- 清理舊匯出檔案 (HTML, Mermaid, JSONL, CSV)
- 清理舊日誌檔案
- 可配置保留天數

**用法**:
```bash
# 清理 30 天前的資料 (預設)
python services/integration/scripts/cleanup.py

# 清理 7 天前的資料
python services/integration/scripts/cleanup.py --days 7

# 僅清理備份檔案
python services/integration/scripts/cleanup.py --backup-only
```

### 4. 🔄 已更新腳本

#### `attack_path_analyzer/engine.py`
- ✅ 使用 `config.ATTACK_GRAPH_FILE` 替代硬編碼路徑
- ✅ 範例代碼更新為標準化配置

#### `attack_path_analyzer/graph_builder.py`
- ✅ 使用 `config.ATTACK_GRAPH_FILE` 和 `config.POSTGRES_DSN`
- ✅ 範例代碼更新為標準化配置

#### `attack_path_analyzer/visualizer.py`
- ✅ 使用 `config.ATTACK_GRAPH_FILE` 替代硬編碼路徑
- ✅ 範例代碼更新為標準化配置

### 5. 🌐 環境變數配置

已在 `.env` 新增以下配置:

```bash
# ================================
# 整合模組配置 (Integration Module)
# ================================

# 整合模組資料儲存根目錄
AIVA_INTEGRATION_DATA_DIR=C:/D/fold7/AIVA-git/data/integration

# 攻擊路徑圖檔案 (NetworkX pickle)
AIVA_ATTACK_GRAPH_FILE=${AIVA_INTEGRATION_DATA_DIR}/attack_paths/attack_graph.pkl

# 經驗資料庫 URL (SQLite)
AIVA_EXPERIENCE_DB_URL=sqlite:///${AIVA_INTEGRATION_DATA_DIR}/experiences/experience.db

# 訓練資料集輸出目錄
AIVA_TRAINING_DATASET_DIR=${AIVA_INTEGRATION_DATA_DIR}/training_datasets

# 模型檢查點目錄
AIVA_MODEL_CHECKPOINT_DIR=${AIVA_INTEGRATION_DATA_DIR}/models
```

### 6. 📚 文件

#### `data/integration/README.md`
詳細說明:
- 目錄結構
- 資料庫說明 (格式、用途、大小、更新頻率、備份策略)
- 配置方式 (環境變數、Python 配置)
- 使用範例 (攻擊路徑引擎、經驗資料庫)
- 備份與維護指引
- 注意事項

#### `services/integration/scripts/README.md`
詳細說明:
- 腳本列表與用法
- 自動化排程建議 (Windows Task Scheduler, Linux Cron)
- 監控建議 (檢查備份狀態、磁碟空間)
- 故障排除指引

---

## 🎯 優化成果

### 標準化管理
- ✅ **統一配置**: 所有路徑由 `config.py` 集中管理
- ✅ **環境變數支援**: 可透過 `.env` 自訂路徑
- ✅ **自動建立目錄**: 配置模組會自動建立所需目錄

### 資料組織
- ✅ **分類清晰**: 攻擊路徑、經驗、訓練資料、模型各自獨立
- ✅ **匯出分離**: 匯出檔案與原始資料分開存放
- ✅ **備份獨立**: 備份目錄與工作目錄分離

### 維護便利
- ✅ **自動備份**: 一鍵備份所有重要資料
- ✅ **自動清理**: 自動清理舊資料,釋放磁碟空間
- ✅ **排程支援**: 提供 Windows 和 Linux 排程範例

### 降低複雜度
- ✅ **零硬編碼路徑**: 所有腳本使用統一配置
- ✅ **簡化部署**: 只需設定環境變數即可
- ✅ **易於測試**: 可透過環境變數切換測試路徑

---

## 📊 資料流向

```
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL (資產與漏洞)                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
              ┌────────────────────────┐
              │   GraphBuilder         │
              │   (圖建構器)            │
              └────────────┬───────────┘
                           │
                           ▼
              ┌────────────────────────┐
              │ AttackPathEngine       │
              │ (攻擊路徑引擎)          │
              └────────────┬───────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  attack_graph.pkl (NetworkX)    │
         │  儲存於: attack_paths/           │
         └─────────────────────────────────┘
                           │
                 ┌─────────┴─────────┐
                 ▼                   ▼
      ┌──────────────────┐  ┌──────────────────┐
      │ Visualizer       │  │ Path Finder      │
      │ (可視化)         │  │ (路徑查詢)        │
      └────────┬─────────┘  └──────────────────┘
               │
               ▼
    ┌───────────────────────┐
    │ exports/              │
    │ - attack_paths.html   │
    │ - attack_paths.mmd    │
    └───────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│              攻擊執行 (AST + Execution Trace)                │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
              ┌────────────────────────┐
              │ ExperienceRepository   │
              │ (經驗資料庫)            │
              └────────────┬───────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │ experience.db (SQLite)          │
         │ 儲存於: experiences/             │
         └─────────────────────────────────┘
                           │
                 ┌─────────┴─────────┐
                 ▼                   ▼
      ┌──────────────────┐  ┌──────────────────┐
      │ Training Dataset │  │ Model Training   │
      │ (資料集建立)      │  │ (模型訓練)        │
      └────────┬─────────┘  └────────┬─────────┘
               │                     │
               ▼                     ▼
    ┌───────────────────┐  ┌──────────────────┐
    │ training_datasets/│  │ models/          │
    │ - dataset_*.jsonl │  │ - attack_*.pth   │
    └───────────────────┘  └──────────────────┘
```

---

## 🔄 建議排程

### 每日任務 (2:00 AM)
```bash
# 備份所有資料
python services/integration/scripts/backup.py
```

### 每週任務 (週日 3:00 AM)
```bash
# 清理 30 天前的舊資料
python services/integration/scripts/cleanup.py --days 30
```

### Windows Task Scheduler 設定
```powershell
# 備份任務
$action = New-ScheduledTaskAction -Execute "python" `
    -Argument "C:\D\fold7\AIVA-git\services\integration\scripts\backup.py" `
    -WorkingDirectory "C:\D\fold7\AIVA-git"
$trigger = New-ScheduledTaskTrigger -Daily -At 2am
Register-ScheduledTask -Action $action -Trigger $trigger `
    -TaskName "AIVA Integration Backup"

# 清理任務
$action = New-ScheduledTaskAction -Execute "python" `
    -Argument "C:\D\fold7\AIVA-git\services\integration\scripts\cleanup.py --days 30" `
    -WorkingDirectory "C:\D\fold7\AIVA-git"
$trigger = New-ScheduledTaskTrigger -Weekly -DaysOfWeek Sunday -At 3am
Register-ScheduledTask -Action $action -Trigger $trigger `
    -TaskName "AIVA Integration Cleanup"
```

---

## 📝 後續建議

### 短期 (1-2 週)
1. **測試備份腳本**: 執行備份並驗證備份檔案完整性
2. **測試清理腳本**: 在測試環境驗證清理邏輯
3. **設定排程任務**: 配置自動備份和清理
4. **監控磁碟空間**: 定期檢查 `data/integration/` 大小

### 中期 (1-2 月)
1. **PostgreSQL 遷移**: 考慮將 experience.db 從 SQLite 遷移至 PostgreSQL (生產環境)
2. **備份到雲端**: 將重要備份上傳至雲端儲存 (AWS S3, Azure Blob)
3. **監控告警**: 設定備份失敗告警通知
4. **資料保留策略**: 根據實際使用情況調整保留天數

### 長期 (3-6 月)
1. **分散式儲存**: 考慮使用分散式儲存系統 (Ceph, MinIO)
2. **資料壓縮**: 實作舊資料自動壓縮功能
3. **增量備份**: 改進為增量備份以節省空間
4. **資料分析**: 建立資料使用分析儀表板

---

## ✅ 驗證檢查清單

- [x] 資料儲存目錄結構已建立
- [x] 配置檔案 (config.py) 已建立
- [x] 備份腳本 (backup.py) 已建立
- [x] 清理腳本 (cleanup.py) 已建立
- [x] 相關腳本已更新使用標準化路徑
- [x] 環境變數配置已加入 .env
- [x] 文件 (README.md) 已建立
- [ ] 備份腳本已測試 (待執行)
- [ ] 清理腳本已測試 (待執行)
- [ ] 排程任務已設定 (待執行)

---

## 🔗 相關文件

- [整合模組 README](services/integration/README.md)
- [資料儲存指南](data/integration/README.md)
- [維護腳本說明](services/integration/scripts/README.md)
- [攻擊路徑分析器](services/integration/aiva_integration/attack_path_analyzer/README.md)
- [資料儲存總指南](guides/development/DATA_STORAGE_GUIDE.md)

---

**完成時間**: 2025-11-16  
**完成者**: GitHub Copilot  
**狀態**: ✅ 已完成
