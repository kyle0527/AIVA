[package]
name = "crypto_engine"
version = "0.1.0"
edition = "2021"
authors = ["AIVA"]
description = "Rust engine for cryptography scanning"
license = "MIT"

[lib]
name = "crypto_engine"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21.2", features = ["extension-module"] }
