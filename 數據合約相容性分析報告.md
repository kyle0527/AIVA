# 🔍 數據合約相容性分析報告

> **分析時間**: 2025年11月30日  
> **分析範圍**: 新版數據合約 vs 現有系統 + aiva_common 規範適用性  
> **結論**: ✅ **完全相容，可共存，aiva_common 規範完全適用**

---

## 📋 目錄

- [執行摘要](#執行摘要)
- [數據合約現狀分析](#數據合約現狀分析)
  - [1. 現有數據合約（已實現）](#1-現有數據合約已實現)
  - [2. 新版數據合約（計劃中）](#2-新版數據合約計劃中)
- [相容性分析](#相容性分析)
  - [兩版本對比](#兩版本對比)
  - [共存策略](#共存策略)
- [aiva_common 規範適用性](#aiva_common-規範適用性)
  - [核心設計原則](#核心設計原則)
  - [完全適用的場景](#完全適用的場景)
  - [特例情況分析](#特例情況分析)
- [AI 單一事實來源](#ai-單一事實來源)
- [實施建議](#實施建議)
- [結論](#結論)

---

## 執行摘要

### 核心發現

1. ✅ **新舊數據合約可完全共存** - 無衝突，可漸進式遷移
2. ✅ **aiva_common 規範 100% 適用** - 無例外情況
3. ✅ **AI 單一事實來源無影響** - 與數據合約架構正交
4. ✅ **無需修改現有代碼** - 新合約為增量補充

### 關鍵結論

**新版數據合約** (`FeatureRequest`/`FeatureResult`) 是為了 **Core → Features 調用** 而設計的補充合約，與現有的 **Features 內部使用** 的 `FeatureResult` (來自 `services/features/base/result_schema.py`) **完全不衝突**。

---

## 數據合約現狀分析

### 1. 現有數據合約（已實現）

#### 1.1 Features 內部使用

**位置**: `services/features/base/result_schema.py`

```python
class FeatureResult(BaseModel):
    """功能模組執行結果（Features 內部使用）"""
    feature_name: str           # 功能模組名稱
    task_id: str                # 任務 ID
    status: FeatureExecutionStatus
    execution_time: float
    findings: List[Finding]     # 發現的漏洞列表
    statistics: Dict[str, Any]
    error_message: Optional[str]
    metadata: Optional[Dict[str, Any]]
```

**使用場景**:
- ✅ Features 模組內部返回
- ✅ Integration Coordinator 接收處理
- ✅ 雙閉環數據收集

**特點**:
- 詳細的執行信息
- 完整的漏洞 Finding 列表
- 豐富的統計數據
- 適合內循環/外循環分析

---

#### 1.2 Integration Coordinator 使用

**位置**: `services/integration/coordinators/base_coordinator.py`

```python
class FeatureResult(BaseModel):
    """Features 模組返回的完整結果（Coordinator 專用）"""
    success: bool
    findings: List[CoordinatorFinding]
    statistics: StatisticsData
    performance: PerformanceMetrics
    errors: List[ErrorInfo]
    metadata: Dict[str, Any]
```

**使用場景**:
- ✅ Coordinator 處理雙閉環
- ✅ 生成 OptimizationData (內循環)
- ✅ 生成 ReportData (外循環)
- ✅ 發送 CoreFeedback 給 Core

---

#### 1.3 aiva_common 標準合約

**位置**: `services/aiva_common/schemas/`

**核心合約**:
```python
# 基礎通信
from aiva_common.schemas import (
    MessageHeader,          # 訊息標頭
    APIResponse,           # 標準響應
    Authentication,        # 認證
)

# 任務管理
from aiva_common.schemas import (
    FunctionTaskPayload,      # 功能任務定義
    FunctionExecutionResult,  # 功能執行結果
    TaskQueue,               # 任務隊列
)

# 漏洞發現
from aiva_common.schemas import (
    FindingPayload,          # 發現結果
    FindingEvidence,         # 證據信息
    Vulnerability,           # 漏洞信息
    UnifiedVulnerabilityFinding,  # 統一漏洞發現
)

# 決策合約
from aiva_common.schemas import (
    HighLevelIntent,         # 高階意圖
    DecisionFeedback,        # 決策反饋
)
```

---

### 2. 新版數據合約（計劃中）

**位置**: `services/aiva_common/schemas/feature_schemas.py` (待創建)

#### 2.1 新增合約定義

```python
class FeatureType(str, Enum):
    """Feature 類型枚舉"""
    XSS = "xss"
    SQLI = "sqli"
    SSRF = "ssrf"
    # ... 更多類型

class FeatureRequest(BaseModel):
    """Core → Features 調用請求"""
    feature_type: FeatureType
    target: HttpUrl
    options: Dict[str, Any] = {}
    
    # 優化參數（從 FeedbackProcessor 傳入）
    concurrency: Optional[int] = None
    timeout_ms: Optional[int] = None
    preferred_payloads: Optional[List[str]] = None

class FeatureResult(BaseModel):
    """Features → Core 返回結果（簡化版）"""
    success: bool
    feature_type: FeatureType
    target: str
    vulnerabilities: List[Vulnerability]
    metadata: Dict[str, Any]
    error: Optional[str] = None
```

**使用場景**:
- ✅ Core 通過 FeaturesInvoker 調用 Features
- ✅ Features 返回簡化結果給 Core
- ✅ 自動觸發 Integration Coordinator 處理

---

## 相容性分析

### 兩版本對比

| 特性 | 現有 FeatureResult (Features 內部) | 新版 FeatureResult (Core ↔ Features) |
|------|-----------------------------------|-------------------------------------|
| **定義位置** | `services/features/base/result_schema.py` | `services/aiva_common/schemas/feature_schemas.py` |
| **使用者** | Features 模組 → Coordinator | Core → Features (通過 FeaturesInvoker) |
| **命名空間** | `from services.features.base import FeatureResult` | `from aiva_common.schemas.feature_schemas import FeatureRequest, FeatureResult` |
| **數據詳細度** | 詳細（含 Finding 對象） | 簡化（含 Vulnerability 列表） |
| **用途** | 內循環/外循環分析 | 快速調用和返回 |
| **關係** | 被 Coordinator 消費 | 觸發 Coordinator 處理 |

---

### 共存策略

#### 方案：分層使用，無衝突

```
┌─────────────────────────────────────────────────────────┐
│  AICommanderV2 (Core)                                    │
│  └─ 使用新版 FeatureRequest/FeatureResult               │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│  FeaturesInvoker                                         │
│  • 接收: FeatureRequest (新版)                           │
│  • 調用: Features 模組                                   │
│  • 返回: FeatureResult (新版，簡化)                      │
│  • 觸發: Integration Coordinator                         │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│  Features 模組 (func_xss, func_sqli, ...)               │
│  • 內部使用: FeatureResult (現有，詳細)                  │
│  • 返回給 FeaturesInvoker: 自動轉換為新版簡化格式        │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│  Integration Coordinator (XSSCoordinator, ...)           │
│  • 接收: 完整的 FeatureResult (現有)                     │
│  • 處理: 雙閉環（OptimizationData + ReportData）        │
│  • 發送: CoreFeedback                                    │
└─────────────────────────────────────────────────────────┘
```

**關鍵點**:
1. ✅ **兩個 FeatureResult 在不同命名空間** - 無名稱衝突
2. ✅ **FeaturesInvoker 做轉換** - 新版 → 現有格式
3. ✅ **Coordinator 不需修改** - 繼續使用現有格式
4. ✅ **Features 模組不需修改** - 內部繼續使用現有格式

---

## aiva_common 規範適用性

### 核心設計原則

**位置**: `services/aiva_common/README.md`

#### 1. 數據合約原則

```markdown
### 數據合約設計原則

1. **Pydantic v2 優先**: 所有合約使用 Pydantic BaseModel
2. **類型安全**: 明確的類型註解和驗證
3. **枚舉優先**: 使用 Enum 而非字符串常量
4. **文檔完整**: 每個字段都有 description
5. **版本控制**: 使用 Field 標記廢棄字段
```

#### 2. Schema 位置規範

```markdown
### Schema 組織結構

services/aiva_common/schemas/
├── _base/              # 核心基礎（所有領域依賴）
├── analysis/           # 分析引擎領域
├── security/           # 安全檢測領域
├── testing/            # 測試執行領域
├── infrastructure/     # 基礎設施領域
├── interfaces/         # 外部接口領域
└── risk/               # 風險評估領域
```

---

### 完全適用的場景

#### ✅ 場景 1: 新增 Schema

**新版數據合約應該遵循 aiva_common 規範**:

```python
# ✅ 正確位置
services/aiva_common/schemas/feature_schemas.py

# ✅ 正確設計
from pydantic import BaseModel, Field, HttpUrl
from aiva_common.enums import Severity, Confidence

class FeatureRequest(BaseModel):
    """Core → Features 調用請求
    
    Compliance:
    - 創建日期: 2025-11-30
    - 用途: 實現 AI 自動化閉環 - Core → Features 調用
    - 符合原則: Pydantic v2, 類型安全, 枚舉優先
    """
    feature_type: FeatureType = Field(description="Feature 類型")
    target: HttpUrl = Field(description="目標 URL")
    options: Dict[str, Any] = Field(
        default_factory=dict,
        description="額外選項"
    )
```

---

#### ✅ 場景 2: 使用現有 Schema

**aiva_common 提供的標準合約完全適用**:

```python
# ✅ 使用標準漏洞合約
from aiva_common.schemas import (
    Vulnerability,           # 標準漏洞定義
    FindingEvidence,         # 證據信息
    UnifiedVulnerabilityFinding,  # 統一漏洞發現
)

# ✅ 使用標準枚舉
from aiva_common.enums import (
    Severity,               # 嚴重程度
    Confidence,             # 置信度
    VulnerabilityType,      # 漏洞類型
)

# ✅ 在新合約中引用
class FeatureResult(BaseModel):
    """Features 返回結果"""
    vulnerabilities: List[Vulnerability]  # 使用標準合約
```

---

#### ✅ 場景 3: 跨語言通信

**aiva_common 規範支持多語言生成**:

```yaml
# core_schema_sot.yaml (單一事實來源)
schemas:
  - name: FeatureRequest
    description: "Core → Features 調用請求"
    fields:
      - name: feature_type
        type: string
        required: true
      - name: target
        type: string
        format: uri
        required: true
```

**自動生成**:
```bash
# 生成 Python
python tools/generate_schemas.py --target python

# 生成 Go
python tools/generate_schemas.py --target go

# 生成 TypeScript
python tools/generate_schemas.py --target typescript

# 生成 Rust
python tools/generate_schemas.py --target rust
```

---

### 特例情況分析

#### ❌ 是否有例外？ → ✅ 無例外

**檢查清單**:

| 檢查項 | 是否適用 | 說明 |
|--------|---------|------|
| Pydantic v2 | ✅ 完全適用 | 新舊合約都使用 Pydantic |
| 類型安全 | ✅ 完全適用 | 所有字段都有類型註解 |
| 枚舉優先 | ✅ 完全適用 | `FeatureType`, `Severity` 等 |
| 文檔完整 | ✅ 完全適用 | 使用 `Field(description=...)` |
| 命名空間 | ✅ 完全適用 | 統一在 `aiva_common.schemas` |
| 跨語言生成 | ✅ 完全適用 | 通過 `core_schema_sot.yaml` |
| 單一事實 | ✅ 完全適用 | YAML 為唯一定義源 |

**結論**: **沒有任何例外情況，aiva_common 規範 100% 適用於整個專案**。

---

## AI 單一事實來源

### 現狀分析

**AI 確實會新建自己的單一事實來源**，但這與數據合約架構 **完全正交**（無衝突）。

#### 兩個不同的"單一事實"

| 維度 | AI 單一事實 | Schema 單一事實 |
|------|------------|----------------|
| **層級** | 業務層 | 數據層 |
| **內容** | AI 決策、策略、知識 | 數據結構定義 |
| **文件** | AI 專用 YAML/JSON | `core_schema_sot.yaml` |
| **使用者** | AI 大腦、規劃器 | 所有模組（跨語言） |
| **關係** | 使用 Schema 定義的合約 | 定義 AI 使用的數據格式 |

---

### 架構示意圖

```
┌───────────────────────────────────────────────────────┐
│  AI 單一事實來源 (業務層)                              │
│  • ai_strategies.yaml                                 │
│  • ai_knowledge_base.json                             │
│  • decision_rules.yaml                                │
│  ──────────────────────────────────────────────────   │
│  內容: AI 決策規則、策略、知識                         │
└───────────────────────────────────────────────────────┘
                        ↓ 使用
┌───────────────────────────────────────────────────────┐
│  Schema 單一事實來源 (數據層)                          │
│  • core_schema_sot.yaml                               │
│  ──────────────────────────────────────────────────   │
│  內容: 數據結構定義                                    │
│  • HighLevelIntent                                    │
│  • FeatureRequest/FeatureResult                       │
│  • OptimizationData/ReportData                        │
└───────────────────────────────────────────────────────┘
                        ↓ 生成
┌───────────────────────────────────────────────────────┐
│  實際代碼 (Python/Go/Rust/TypeScript)                 │
│  • services/aiva_common/schemas/*.py                  │
│  • services/features/common/go/schemas/*.go           │
│  • services/features/common/rust/schemas/*.rs         │
│  • services/features/common/typescript/schemas/*.ts   │
└───────────────────────────────────────────────────────┘
```

---

### 為什麼不衝突？

#### 1. 不同的抽象層級

```python
# AI 單一事實: 業務決策
ai_strategy = {
    "vulnerability_priority": ["CRITICAL", "HIGH"],
    "scan_strategy": "depth_first",
    "time_budget_seconds": 3600
}

# Schema 單一事實: 數據格式
class HighLevelIntent(BaseModel):
    """AI 決策的數據格式定義"""
    intent_type: IntentType
    target: TargetInfo
    parameters: Dict[str, Any]  # ← AI 決策放這裡
```

**關鍵**: AI 決策存在 `parameters` 字段中，Schema 只定義字段類型。

---

#### 2. 各自的職責

| 職責 | AI 單一事實 | Schema 單一事實 |
|------|------------|----------------|
| **定義什麼** | 業務邏輯、決策規則 | 數據結構、字段類型 |
| **回答問題** | "該做什麼決策？" | "數據長什麼樣？" |
| **變更頻率** | 頻繁（AI 學習更新） | 穩定（接口契約） |
| **影響範圍** | AI 模組 | 所有模組 |

---

## 實施建議

### Phase 1: 新增新版數據合約 (1 天)

**不影響現有代碼**:

```python
# 1. 創建新文件
services/aiva_common/schemas/feature_schemas.py

# 2. 定義新合約
class FeatureRequest(BaseModel):
    """Core → Features 調用請求"""
    # ... (如前所述)

class FeatureResult(BaseModel):
    """Features → Core 返回結果（簡化版）"""
    # ... (如前所述)

# 3. 在 __init__.py 中導出
from .feature_schemas import FeatureRequest, FeatureResult as CoreFeatureResult
```

**命名策略**:
```python
# 方案 1: 使用別名
from aiva_common.schemas.feature_schemas import FeatureResult as CoreFeatureResult
from services.features.base import FeatureResult as InternalFeatureResult

# 方案 2: 不同模組自然隔離
# Core 使用:
from aiva_common.schemas import FeatureRequest, FeatureResult  # 新版

# Features 使用:
from services.features.base import FeatureResult  # 現有
```

---

### Phase 2: 實現 FeaturesInvoker (2-3 天)

**自動轉換格式**:

```python
class FeaturesInvoker:
    async def invoke_feature(
        self, request: FeatureRequest  # 新版
    ) -> FeatureResult:  # 新版（簡化）
        
        # 調用 Features 模組（返回現有格式）
        internal_result: InternalFeatureResult = await feature.scan(...)
        
        # 轉換為新版簡化格式
        return FeatureResult(
            success=internal_result.status == "success",
            feature_type=request.feature_type,
            target=str(request.target),
            vulnerabilities=[
                self._convert_finding_to_vulnerability(f)
                for f in internal_result.findings
            ],
            metadata=internal_result.statistics
        )
```

---

### Phase 3: 更新 core_schema_sot.yaml (半天)

**添加新合約定義**:

```yaml
schemas:
  # 新增: Core → Features 調用合約
  - name: FeatureRequest
    description: "Core → Features 調用請求"
    module: feature_schemas
    fields:
      - name: feature_type
        type: string
        enum: FeatureType
        required: true
        description: "Feature 類型"
      
      - name: target
        type: string
        format: uri
        required: true
        description: "目標 URL"
      
      - name: options
        type: object
        required: false
        description: "額外選項"

  - name: FeatureResult
    description: "Features → Core 返回結果（簡化版）"
    module: feature_schemas
    fields:
      - name: success
        type: boolean
        required: true
      
      - name: vulnerabilities
        type: array
        items:
          $ref: "#/schemas/Vulnerability"
```

---

## 結論

### ✅ 相容性評估

| 評估項 | 結論 | 說明 |
|--------|------|------|
| **新舊合約衝突** | ✅ 無衝突 | 不同命名空間，不同用途 |
| **共存可行性** | ✅ 完全可行 | 分層使用，自動轉換 |
| **aiva_common 適用性** | ✅ 100% 適用 | 無任何例外 |
| **AI 單一事實影響** | ✅ 無影響 | 業務層 vs 數據層，正交關係 |
| **現有代碼修改** | ✅ 無需修改 | 增量補充，向後相容 |

---

### 🎯 核心結論

1. **新版數據合約是增量補充**
   - 用於 Core → Features 調用
   - 不替換現有的 Features 內部合約
   - 完全共存，無衝突

2. **aiva_common 規範完全適用**
   - 無任何例外情況
   - 所有 Schema 都應放在 `services/aiva_common/schemas/`
   - 使用 `core_schema_sot.yaml` 作為單一事實來源

3. **AI 單一事實來源無影響**
   - AI 業務邏輯與數據合約正交
   - AI 使用 Schema 定義的數據格式
   - 各自維護自己的單一事實

4. **實施策略清晰**
   - Phase 1: 新增合約（1 天）
   - Phase 2: 實現 FeaturesInvoker（2-3 天）
   - Phase 3: 更新 YAML（半天）
   - **總計**: 3.5-4.5 天完成

---

### 📊 最終答案

**問題 1**: 新版數據合約跟現存的相容性如何？
- ✅ **完全相容，可共存**

**問題 2**: 共存還是替換？
- ✅ **共存**，新版是補充而非替換

**問題 3**: AI 單一事實來源有影響嗎？
- ✅ **沒差**，業務層與數據層正交

**問題 4**: aiva_common 規範是否完全適用？
- ✅ **100% 適用**，整個專案無例外

---

## 附錄：快速參考

### 數據合約速查表

| 合約名稱 | 位置 | 用途 | 使用者 |
|---------|------|------|-------|
| `FeatureResult` (現有) | `services/features/base/` | Features 內部 | Features → Coordinator |
| `FeatureRequest` (新) | `services/aiva_common/schemas/` | Core 調用 | Core → Features |
| `FeatureResult` (新) | `services/aiva_common/schemas/` | Core 接收 | Features → Core |
| `HighLevelIntent` | `services/aiva_common/schemas/` | AI 決策 | AI → Task Planning |
| `OptimizationData` | `services/integration/coordinators/` | 內循環 | Coordinator → Core |
| `ReportData` | `services/integration/coordinators/` | 外循環 | Coordinator → Report |

### 導入速查

```python
# Core 使用
from aiva_common.schemas import FeatureRequest, FeatureResult

# Features 使用
from services.features.base import FeatureResult, Finding

# Coordinator 使用
from services.integration.coordinators.base_coordinator import (
    FeatureResult,
    OptimizationData,
    ReportData,
    CoreFeedback,
)

# 標準合約
from aiva_common.schemas import (
    Vulnerability,
    FindingEvidence,
    UnifiedVulnerabilityFinding,
)

# 標準枚舉
from aiva_common.enums import (
    Severity,
    Confidence,
    VulnerabilityType,
)
```

---

**報告完成** ✅
