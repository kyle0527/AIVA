# AIVA Docker 環境配置 (容器內運行)
# 此配置用於在 Docker Compose 網絡內運行的服務

# ================================
# 基本配置
# ================================
ENVIRONMENT=docker
MODE=development
OFFLINE_MODE=false
LOG_LEVEL=INFO
DEBUG=false
AUTO_MIGRATE=1

# ================================
# 資料庫配置 (PostgreSQL + pgvector)
# ================================
DATABASE_URL=postgresql://aiva:aiva_secure_password@postgres:5432/aiva
DB_TYPE=postgres

# PostgreSQL 詳細配置
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=aiva
POSTGRES_USER=aiva
POSTGRES_PASSWORD=aiva_secure_password

# ================================
# 消息隊列配置 (RabbitMQ)
# ================================
RABBITMQ_URL=amqp://aiva:aiva_mq_password@rabbitmq:5672/aiva
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=aiva
RABBITMQ_PASSWORD=aiva_mq_password
RABBITMQ_VHOST=aiva

MQ_EXCHANGE=aiva.topic
MQ_DLX=aiva.dlx

# ================================
# Redis 配置
# ================================
REDIS_URL=redis://:aiva_redis_password@redis:6379/0
REDIS_HOST=redis
REDIS_PORT=6379

# ================================
# Neo4j 配置
# ================================
NEO4J_URL=bolt://neo4j:password@neo4j:7687
NEO4J_HOST=neo4j
NEO4J_PORT=7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# ================================
# API 配置
# ================================
API_KEY=dev_api_key_for_docker_testing
INTEGRATION_TOKEN=docker_integration_token

# ================================
# 監控配置
# ================================
ENABLE_PROM=1
RATE_LIMIT_RPS=50
RATE_LIMIT_BURST=100

# ================================
# CORS 配置
# ================================
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://aiva-core:8000