# AIVA Docker 環境配置 (容器內運行)
# 此配置用於在 Docker Compose 網絡內運行的服務

# ================================
# 基本配置
# ================================
AIVA_ENVIRONMENT=docker
AIVA_MODE=development
AIVA_OFFLINE_MODE=false
AIVA_LOG_LEVEL=INFO
AIVA_DEBUG=false
AUTO_MIGRATE=1

# ================================
# 資料庫配置 (PostgreSQL + pgvector)
# ================================
AIVA_DATABASE_URL=postgresql://postgres:aiva123@postgres:5432/aiva_db
AIVA_DB_TYPE=postgres

# PostgreSQL 詳細配置
AIVA_POSTGRES_HOST=postgres
AIVA_POSTGRES_PORT=5432
AIVA_POSTGRES_DB=aiva_db
AIVA_POSTGRES_USER=postgres
AIVA_POSTGRES_PASSWORD=aiva123

# 傳統配置支援
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=aiva_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=aiva123

# ================================
# 消息隊列配置 (RabbitMQ)
# ================================
AIVA_RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
AIVA_RABBITMQ_HOST=rabbitmq
AIVA_RABBITMQ_PORT=5672
AIVA_RABBITMQ_USER=guest
AIVA_RABBITMQ_PASSWORD=guest
AIVA_RABBITMQ_VHOST=/

AIVA_MQ_EXCHANGE=aiva.topic
AIVA_MQ_DLX=aiva.dlx

# ================================
# Redis 配置
# ================================
AIVA_REDIS_URL=redis://redis:6379/0
AIVA_REDIS_HOST=redis
AIVA_REDIS_PORT=6379

# ================================
# Neo4j 配置
# ================================
AIVA_NEO4J_URL=bolt://neo4j:aiva1234@neo4j:7687
AIVA_NEO4J_HOST=neo4j
AIVA_NEO4J_PORT=7687
AIVA_NEO4J_USER=neo4j
AIVA_NEO4J_PASSWORD=aiva1234

# ================================
# API 配置
# ================================
AIVA_API_KEY=dev_api_key_for_docker_testing
AIVA_INTEGRATION_TOKEN=docker_integration_token

# ================================
# 監控配置
# ================================
AIVA_ENABLE_PROM=1
AIVA_RATE_LIMIT_RPS=50
AIVA_RATE_LIMIT_BURST=100

# ================================
# CORS 配置
# ================================
AIVA_CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://aiva-core:8000