# AIVA AI 核心功能需求分析與參數分配建議報告

> **報告日期**: 2025年11月9日  
> **當前狀態**: 3.17M 參數（隨機權重，無訓練功能）  
> **建議方案**: 方案 C - 模組化架構（28.99M 參數）  
> **資料庫部署**: services/integration/（統一管理）

---

## 📋 執行摘要

根據對 AIVA 文件和實際代碼的深入分析，當前 AI 核心存在以下關鍵問題：

### ❌ **核心問題**
1. **無訓練功能** - 權重為隨機初始化，準確率僅 ~5%
2. **RAG 知識庫為空** - 框架完整但缺乏實際知識
3. **無記憶機制** - 無法記住歷史決策
4. **無注意力機制** - 無法聚焦關鍵特徵
5. **輸入輸出受限** - 固定 512 維輸入，僅 20 個工具輸出

### ✅ **建議方案**
- **推薦**: 方案 C（模組化方案）
- **參數規模**: 28.99M 參數（110 MB float32）
- **開發時程**: 8 週完成
- **預期準確率**: 70-85%（訓練後）

---

## 🎯 第一部分：AI 核心功能需求總覽

基於文件分析，AIVA AI 需要具備以下 6 大核心能力：

### 1️⃣ **決策與推理能力** 🔴 極高優先級

**功能描述**:
- 智能工具選擇（Tool Selection）
- 攻擊路徑規劃（Attack Path Planning）
- 風險評估與判斷（Risk Assessment）
- 多專家協同決策（Multi-Expert Decision）

**當前狀態**: 架構存在，決策邏輯簡陋  
**問題**: BioNeuron 模型使用隨機權重，無實際決策能力

### 2️⃣ **知識檢索與增強 (RAG)** 🟠 高優先級

**功能描述**:
- 代碼庫索引與檢索（Code Indexing）
- 漏洞知識庫查詢（Vulnerability Knowledge）
- 歷史經驗檢索（Experience Retrieval）
- 上下文增強決策（Context Augmentation）

**當前狀態**: 框架完整，知識庫為空  
**問題**: 缺乏向量化嵌入層，無實際知識數據

### 3️⃣ **學習與進化能力** 🔴 極高優先級

**功能描述**:
- 經驗記錄與保存（Experience Logging）
- 反向傳播訓練（Backpropagation Training）
- 模型權重更新（Weight Updating）
- 持續學習機制（Continual Learning）

**當前狀態**: 無訓練功能，權重隨機  
**問題**: 這是最嚴重的問題，AI 無法學習改進

### 4️⃣ **攻擊計畫執行** 🟡 中優先級

**功能描述**:
- 計畫生成（Plan Generation）
- 任務分解（Task Decomposition）
- 執行監控（Execution Monitoring）
- AST-Trace 對比（Comparison Analysis）

**當前狀態**: 基礎架構存在  
**備註**: `AttackOrchestrator` 和 `ExecutionMonitor` 已實現

### 5️⃣ **跨語言程式操控** 🟢 中低優先級

**功能描述**:
- Python 模組調度（Python Orchestration）
- Go 高性能掃描（Go Performance）
- Rust 底層操作（Rust Low-Level）
- 統一命令介面（Unified Interface）

**當前狀態**: 架構完整，協調簡陋  
**備註**: MCP 合約架構已建立

### 6️⃣ **抗幻覺與驗證** 🟢 中低優先級

**功能描述**:
- 信心度評估（Confidence Scoring）
- 多層驗證機制（Multi-Layer Validation）
- 一致性檢查（Consistency Check）
- 結果可靠性保證（Reliability）

**當前狀態**: 基礎機制存在  
**備註**: `AntiHallucinationModule` 已實現基礎驗證

---

## 📊 第二部分：當前架構參數分佈分析

### 當前架構（bio_neuron_core.py）

```python
class ScalableBioNet:
    def __init__(self, input_size=512, num_tools=20):
        self.fc1 = np.random.randn(512, 2048)           # 1,048,576 參數
        self.spiking1 = BiologicalSpikingLayer(2048, 1024)  # 2,097,152 參數
        self.fc2 = np.random.randn(1024, 20)            # 20,480 參數
```

| 層名稱 | 形狀 | 參數數量 | 佔比 | 用途 |
|--------|------|----------|------|------|
| FC1 | 512 × 2048 | 1,048,576 | 33.1% | 特徵提取（掃描結果 → 高維特徵） |
| BiologicalSpiking | 2048 × 1024 | 2,097,152 | 66.3% | 決策核心（生物神經元模擬） |
| FC2 | 1024 × 20 | 20,480 | 0.6% | 工具選擇（決策 → 20個工具） |

**總計**: 3,166,208 參數（約 3.17M）  
**大小**: 12.08 MB (float32)  
**推理延遲**: 0.890 ms（含 BiologicalSpiking）

---

## ⚠️ 第三部分：問題診斷

### 1. 輸入維度固定 (512) 🔴 嚴重

**影響**: 無法處理複雜的掃描結果和上下文資訊
- Nmap 掃描結果通常包含 100+ 端口資訊
- SQLmap 測試需要編碼 HTTP 請求/響應
- 目標環境上下文（OS、服務、版本）需要額外維度

**建議**: 擴展到 768-1024 維

### 2. 輸出僅 20 個工具 🟠 中等

**影響**: 限制了可擴展性，未來工具增加會受限
- 當前 HackingTool 整合了 50+ 工具
- Feature modules 包含多個檢測模組
- 需要支援工具組合策略

**建議**: 增加到 50-100 個輸出

### 3. 無專門的 RAG 嵌入層 🔴 嚴重

**影響**: 知識檢索效果差，無法有效利用知識庫
- 當前 RAG 檢索使用簡單的 hash 模擬
- 無法理解語義相似性
- 無法檢索相關漏洞知識

**建議**: 添加 512 維專用嵌入層

### 4. 無記憶機制 🔴 嚴重

**影響**: 無法記住歷史決策，無法學習改進
- 每次決策都是獨立的
- 無法利用過往成功經驗
- 無法形成攻擊序列

**建議**: 添加 LSTM/GRU 層（512 維）

### 5. 無注意力機制 🟡 中等

**影響**: 無法聚焦關鍵特徵，決策效率低
- 所有特徵權重相同
- 無法識別關鍵漏洞指標
- 難以處理長序列輸入

**建議**: 添加多頭注意力（8 heads）

---

## 🏗️ 第四部分：參數分配建議方案

### 方案 A: 保守升級方案（8.05M 參數，30.70 MB）

適用於快速原型和資源受限環境。

```
輸入處理層:    768 × 1536     = 1,179,648 參數 (0.46M)
RAG 嵌入層:     512 × 1024     = 524,288 參數 (0.20M)
LSTM 記憶層:    1536 × 768 (4層) = 4,718,592 參數 (1.84M)
BiologicalSpiking: 1536 × 1024 = 1,572,864 參數 (0.61M)
工具選擇層:     1024 × 50      = 51,200 參數 (0.02M)
─────────────────────────────────────────────────────
總計:                            8,046,592 參數 (8.05M)
```

**優點**: 
- ✅ 參數控制合理
- ✅ 記憶體友好（30.70 MB）
- ✅ 推理速度快（~1.2 ms）

**缺點**: 
- ❌ 無注意力機制
- ❌ RAG 能力有限

---

### 方案 B: 積極升級方案（24.53M 參數，93.59 MB）

適用於性能優先、資源充足的環境。

```
多源輸入編碼:    1024 × 2048     = 2,097,152 參數 (0.79M)
RAG 雙向嵌入:    768 × 1536 (2層) = 2,359,296 參數 (0.91M)
多頭注意力:      2048 × 2048 (×3) = 12,582,912 參數 (4.92M)
BiLSTM 記憶:     2048 × 1024 (雙向) = 4,194,304 參數 (1.64M)
BiologicalSpiking: 2048 × 1536   = 3,145,728 參數 (1.23M)
專家路由層:      1536 × 100      = 153,600 參數 (0.06M)
─────────────────────────────────────────────────────
總計:                             24,532,992 參數 (24.53M)
```

**優點**: 
- ✅ 多頭注意力機制
- ✅ 雙向記憶能力
- ✅ RAG 能力強

**缺點**: 
- ⚠️ 推理稍慢（~2-3 ms）
- ⚠️ 訓練成本高

---

### 方案 C: 模組化方案（28.99M 參數，110.59 MB）⭐ 推薦

適用於生產環境，提供最佳的功能性和可維護性。

#### 🔍 感知模組 (Perception Module)

處理多源輸入並編碼為統一表示。

```
掃描結果編碼器:  1024 × 1536    = 1,572,864 參數 (0.61M)
  用途: 編碼 Nmap/SQLmap/等掃描工具的輸出

上下文編碼器:    768 × 1024     = 786,432 參數 (0.30M)
  用途: 編碼目標資訊（OS、服務、版本等）

歷史編碼器:      512 × 768      = 393,216 參數 (0.15M)
  用途: 編碼過往攻擊記錄和成功案例
```

**小計**: 2,752,512 參數（1.05M）

#### 🧠 RAG 模組 (Knowledge Module)

實現知識檢索增強生成。

```
查詢嵌入層:      1024 × 768     = 786,432 參數 (0.30M)
  用途: 將當前任務轉換為查詢向量

文檔嵌入層:      1024 × 768     = 786,432 參數 (0.30M)
  用途: 將知識庫文檔向量化

相似度層:        768 × 512      = 393,216 參數 (0.15M)
  用途: 計算查詢與文檔的相似度並排序
```

**小計**: 1,966,080 參數（0.75M）

#### 💭 決策模組 (Decision Module)

核心決策引擎，整合多種機制。

```
多頭注意力 (8 heads):
  Q/K/V 映射:    2048 × 2048 (×3) = 12,582,912 參數 (4.92M)
  用途: 聚焦關鍵特徵，實現上下文感知

BiLSTM 記憶:     2048 × 1536 (雙向) = 6,291,456 參數 (2.46M)
  用途: 維護攻擊序列的歷史狀態

BiologicalSpiking: 2048 × 1536  = 3,145,728 參數 (1.23M)
  用途: 生物啟發式決策核心

決策融合層:      1536 × 1024    = 1,572,864 參數 (0.61M)
  用途: 融合多源決策信號
```

**小計**: 23,592,960 參數（9.22M）

#### 🎯 執行模組 (Execution Module)

將決策轉換為具體的執行指令。

```
工具選擇器:      1024 × 100     = 102,400 參數 (0.04M)
  用途: 從 100 種工具中選擇最適合的

參數生成器:      1024 × 512     = 524,288 參數 (0.20M)
  用途: 生成工具執行所需的參數

策略輸出器:      1024 × 50      = 51,200 參數 (0.02M)
  用途: 輸出 50 種攻擊策略的組合
```

**小計**: 677,888 參數（0.26M）

---

#### 方案 C 總計

```
感知模組:    2,752,512 參數 (1.05M)
RAG 模組:    1,966,080 參數 (0.75M)
決策模組:   23,592,960 參數 (9.22M)
執行模組:      677,888 參數 (0.26M)
─────────────────────────────────────────────
總計:       28,989,440 參數 (28.99M)
記憶體:     110.59 MB (float32)
```

### 方案 C 優勢分析

#### ✅ **架構優勢**

1. **模組清晰分工**
   - 每個模組職責明確
   - 符合 AIVA 五大模組架構
   - 便於團隊協作開發

2. **獨立開發測試**
   - 可單獨測試感知模組的編碼效果
   - 可獨立訓練 RAG 模組
   - 降低開發風險

3. **熱插拔支援**
   - 可替換任意模組（如升級注意力機制）
   - 不影響其他模組正常運行
   - 便於後續優化

4. **性能與功能平衡**
   - 推理延遲: 1.5-2.5 ms（可接受）
   - 記憶體: 150-200 MB（含資料庫快取）
   - 功能完整: RAG + 記憶 + 注意力

#### ⚠️ **潛在挑戰**

1. **架構複雜**
   - 需要更多工程化工作
   - 模組間介面設計重要
   - 需要完善的測試框架

2. **訓練成本**
   - 需要更多訓練數據
   - 訓練時間較長（每 epoch 5-10 分鐘）
   - 需要 GPU 加速（可選）

---

## 🗃️ 第五部分：資料庫規劃（整合模組）

根據您的要求，所有資料庫統一部署在 `services/integration/` 模組下。

### 1. 📊 經驗資料庫 (ExperienceDB)

**位置**: `services/integration/aiva_integration/reception/`  
**類型**: PostgreSQL / SQLite  
**用途**: 存儲 AI 學習經驗、訓練數據、模型版本

#### 表結構

```sql
-- 經驗記錄主表
experience_records:
  - experience_id (PK)
  - plan_id
  - attack_type
  - target_info (JSON)
  - ast_graph (JSON)
  - execution_trace (JSON)
  - metrics_detail (JSON)
  - reward (FLOAT)
  - feedback_data (JSON)
  - created_at (TIMESTAMP)

-- 訓練數據集
training_datasets:
  - dataset_id (PK)
  - dataset_name
  - description
  - created_at (TIMESTAMP)

-- 數據樣本
dataset_samples:
  - sample_id (PK)
  - dataset_id (FK)
  - experience_id (FK)
  - features (JSON)
  - labels (JSON)
  - quality_score (FLOAT)

-- 模型訓練歷史
model_training_history:
  - training_id (PK)
  - dataset_id (FK)
  - model_version
  - training_params (JSON)
  - accuracy (FLOAT)
  - loss (FLOAT)
  - trained_at (TIMESTAMP)
```

**預估大小**: 初始 50MB，成長到 500MB-1GB  
**備註**: 這是 `ExperienceRepository` 類的後端存儲

---

### 2. 🎯 攻擊知識庫 (KnowledgeDB)

**位置**: `services/integration/aiva_integration/knowledge/`  
**類型**: Vector DB (Chroma/FAISS) + PostgreSQL  
**用途**: RAG 知識檢索、漏洞查詢、攻擊策略

#### 表結構

```sql
-- 漏洞知識（元數據）
vulnerability_knowledge:
  - vuln_id (PK)
  - cve_id
  - vuln_type (SQLi, XSS, etc.)
  - description
  - severity
  - exploitation_technique
  - created_at (TIMESTAMP)

-- 攻擊模式
attack_patterns:
  - pattern_id (PK)
  - pattern_name
  - attack_type
  - success_rate (FLOAT)
  - complexity_level
  - prerequisites (JSON)
  - steps (JSON)

-- 工具文檔
tool_documentation:
  - doc_id (PK)
  - tool_name
  - usage_examples
  - parameters (JSON)
  - best_practices

-- 代碼片段（向量化）
code_snippets:
  - snippet_id (PK)
  - code_content (TEXT)
  - embedding (VECTOR)  -- 向量數據庫字段
  - language
  - purpose
  - tags (ARRAY)
```

**向量數據庫**:
```python
# 使用 Chroma 或 FAISS
collection = chroma_client.create_collection(
    name="aiva_knowledge",
    embedding_function=embedding_fn,
    metadata={"description": "AIVA vulnerability knowledge"}
)
```

**預估大小**: 
- 向量索引: 100-500MB
- 元數據: 50MB

**備註**: 支援 RAG 模組的語義檢索

---

### 3. 📝 執行追蹤庫 (TraceDB)

**位置**: `services/integration/aiva_integration/execution/`  
**類型**: Time-Series DB (InfluxDB) 或 PostgreSQL  
**用途**: 攻擊計畫執行追蹤、性能監控

#### 表結構

```sql
-- 執行軌跡
execution_traces:
  - trace_id (PK)
  - trace_session_id
  - plan_id
  - task_sequence (JSON)
  - execution_path (JSON)
  - start_time (TIMESTAMP)
  - end_time (TIMESTAMP)

-- 任務執行記錄
task_executions:
  - execution_id (PK)
  - trace_id (FK)
  - task_id
  - tool_name
  - parameters (JSON)
  - result (JSON)
  - success (BOOLEAN)
  - execution_time_ms (INT)
  - timestamp (TIMESTAMP)

-- 性能指標
performance_metrics:
  - metric_id (PK)
  - trace_id (FK)
  - metric_name
  - metric_value (FLOAT)
  - timestamp (TIMESTAMP)

-- 錯誤日誌
error_logs:
  - error_id (PK)
  - trace_id (FK)
  - error_type
  - error_message (TEXT)
  - stack_trace (TEXT)
  - timestamp (TIMESTAMP)
```

**時序數據特性**:
- 支援高頻寫入（每秒數百條）
- 支援時間範圍查詢
- 自動數據保留策略（保留 30 天）

**預估大小**: 時序數據 200MB，索引 50MB  
**備註**: 支援 `ExecutionMonitor` 和 `ASTTraceComparator`

---

### 4. 🔧 工具註冊庫 (CapabilityDB)

**位置**: `services/integration/capability/`  
**類型**: PostgreSQL  
**用途**: 工具發現、能力註冊、使用統計

#### 表結構

```sql
-- 能力註冊
capability_records:
  - capability_id (PK)
  - name
  - version
  - description
  - provider (HackingTool, Custom, etc.)
  - status (active, inactive, deprecated)
  - registered_at (TIMESTAMP)

-- 工具元數據
tool_metadata:
  - tool_id (PK)
  - capability_id (FK)
  - tool_name
  - tool_type
  - language (Python, Go, Rust)
  - dependencies (JSON)
  - configuration (JSON)

-- 能力證據
capability_evidence:
  - evidence_id (PK)
  - capability_id (FK)
  - evidence_type
  - evidence_data (JSON)
  - confidence (FLOAT)
  - verified (BOOLEAN)
  - created_at (TIMESTAMP)

-- 使用統計
usage_statistics:
  - stat_id (PK)
  - tool_id (FK)
  - usage_count (INT)
  - success_count (INT)
  - failure_count (INT)
  - avg_execution_time_ms (FLOAT)
  - last_used_at (TIMESTAMP)
```

**預估大小**: 50MB（相對穩定）  
**備註**: 支援 `CapabilityRegistry` 和工具發現系統

---

### 資料庫部署總覽

| 資料庫 | 類型 | 位置 | 預估大小 | 用途 |
|--------|------|------|----------|------|
| ExperienceDB | PostgreSQL/SQLite | `reception/` | 50MB → 500MB | AI 學習經驗 |
| KnowledgeDB | Vector DB + PG | `knowledge/` | 100-500MB | RAG 知識檢索 |
| TraceDB | InfluxDB/PG | `execution/` | 200MB | 執行追蹤 |
| CapabilityDB | PostgreSQL | `capability/` | 50MB | 工具註冊 |

**💾 總容量預估**: 初始 500MB，成長到 2-3GB  
**📍 部署位置**: `services/integration/`（統一管理）

#### 資料庫選型建議

**開發階段**:
```python
# SQLite 快速原型
DATABASE_URL = "sqlite:///./aiva_dev.db"
```

**生產環境**:
```python
# PostgreSQL 主資料庫
DATABASE_URL = "postgresql://aiva:password@localhost:5432/aiva"

# Chroma 向量資料庫
VECTOR_DB_PATH = "/data/aiva/chroma"

# InfluxDB 時序資料庫（可選）
INFLUX_URL = "http://localhost:8086"
```

---

## 🛣️ 第六部分：實施路線圖

### 第一階段 (Week 1-2) 🔴 極高優先級

#### 目標: 實現可訓練的 AI 核心

**任務清單**:
- ☐ 添加反向傳播訓練功能
  ```python
  class TrainableBioNet(ScalableBioNet):
      def backward(self, loss):
          # 實現梯度計算
      def update_weights(self, gradients):
          # Adam 優化器
  ```

- ☐ 實現數據收集器
  ```python
  class TrainingDataCollector:
      def collect_experience(self, task, decision, result):
          # 記錄成功/失敗案例
      def export_dataset(self):
          # 導出訓練數據集
  ```

- ☐ 建立經驗資料庫表結構
  ```sql
  CREATE TABLE experience_records (...);
  CREATE TABLE training_datasets (...);
  ```

- ☐ 完成方案 A 架構改造
  - 擴展輸入層到 768 維
  - 添加 RAG 嵌入層 (512 → 1024)
  - 添加 LSTM 層 (1536 → 768)

**預期產出**: 可訓練的 AI 核心（5M 參數）

**驗證標準**: 
- ✅ 訓練循環可正常運行
- ✅ 損失函數正常下降
- ✅ 準確率從 5% 提升到 40%+

---

### 第二階段 (Week 3-4) 🟠 高優先級

#### 目標: RAG 增強決策系統

**任務清單**:
- ☐ 建立 RAG 知識庫（向量數據庫）
  ```python
  import chromadb
  client = chromadb.Client()
  collection = client.create_collection("aiva_knowledge")
  ```

- ☐ 實現知識檢索與嵌入
  ```python
  class RAGModule:
      def embed_query(self, query):
          # 查詢向量化
      def retrieve_knowledge(self, query_embedding, top_k=5):
          # 相似度檢索
  ```

- ☐ 收集漏洞知識與攻擊模式
  - CVE 數據庫
  - OWASP Top 10
  - HackingTool 工具文檔
  - 成功攻擊案例

- ☐ 完成 RAG 模組整合
  - 查詢嵌入層 (1024 → 768)
  - 文檔嵌入層 (1024 → 768)
  - 相似度計算層 (768 → 512)

**預期產出**: RAG 增強的決策系統

**驗證標準**: 
- ✅ 知識檢索準確率 70%+
- ✅ 相關知識成功應用於決策
- ✅ 決策準確率提升到 55%+

---

### 第三階段 (Week 5-6) 🟡 中優先級

#### 目標: 序列決策系統

**任務清單**:
- ☐ 添加 LSTM 記憶機制
  ```python
  class DecisionModule:
      def __init__(self):
          self.lstm = BiLSTM(input_size=2048, hidden_size=1536)
      def forward_with_memory(self, x, hidden_state):
          # 維護歷史狀態
  ```

- ☐ 實現歷史決策追蹤
  ```python
  class HistoryTracker:
      def track_decision(self, decision, context):
          # 記錄決策歷史
      def get_relevant_history(self, current_context):
          # 檢索相關歷史
  ```

- ☐ 建立執行追蹤資料庫
  ```sql
  CREATE TABLE execution_traces (...);
  CREATE TABLE task_executions (...);
  ```

- ☐ 完成方案 B/C 架構升級
  - BiLSTM 層 (2048 → 1536)
  - 歷史編碼器 (512 → 768)
  - 執行追蹤整合

**預期產出**: 具備記憶的序列決策系統

**驗證標準**: 
- ✅ 能記住過往 10 步決策
- ✅ 序列任務準確率 60%+
- ✅ 攻擊路徑規劃合理

---

### 第四階段 (Week 7-8) 🟢 中低優先級

#### 目標: 完整的模組化 AI 系統

**任務清單**:
- ☐ 添加多頭注意力機制
  ```python
  class MultiHeadAttention:
      def __init__(self, d_model=2048, num_heads=8):
          self.Q = Linear(d_model, d_model)
          self.K = Linear(d_model, d_model)
          self.V = Linear(d_model, d_model)
  ```

- ☐ 實現模組化架構
  - 感知模組（Perception）
  - RAG 模組（Knowledge）
  - 決策模組（Decision）
  - 執行模組（Execution）

- ☐ 完成所有資料庫整合
  - ExperienceDB ✅
  - KnowledgeDB ✅
  - TraceDB ✅
  - CapabilityDB ✅

- ☐ 性能優化與測試
  - 推理速度優化（目標 < 2.5 ms）
  - 批次處理優化
  - 記憶體使用優化
  - 完整的單元測試

**預期產出**: 完整的模組化 AI 系統（方案 C）

**驗證標準**: 
- ✅ 綜合準確率 70-85%
- ✅ 推理延遲 < 2.5 ms
- ✅ 所有模組正常運行
- ✅ 資料庫整合完成

---

## 📊 第七部分：總結與建議

### 🎯 推薦方案

**方案 C: 模組化方案（28.99M 參數，110 MB）**

#### 選擇理由

1. **✅ 清晰的模組分工**
   - 符合 AIVA 五大模組架構（Core, Scan, Integration, Features, Common）
   - 每個模組職責明確，便於維護
   - 符合單一職責原則

2. **✅ 可獨立開發測試**
   - 感知模組可單獨測試編碼效果
   - RAG 模組可獨立訓練和驗證
   - 決策模組可獨立優化
   - 降低開發風險

3. **✅ 支援熱插拔**
   - 可替換任意模組（如升級注意力機制）
   - 不影響其他模組正常運行
   - 便於後續優化和擴展

4. **✅ 參數規模合理**
   - 28.99M 參數（110 MB float32）
   - 在大多數服務器上可運行
   - 支援 GPU 加速（可選）

5. **✅ 功能完整**
   - 包含完整的 RAG 能力
   - 包含記憶機制（BiLSTM）
   - 包含注意力機制（8 heads）
   - 支援所有核心功能

---

### 🗃️ 資料庫部署建議

**開發階段**:
```bash
# 使用 SQLite 快速原型
DATABASE_URL="sqlite:///./aiva_dev.db"
```

**生產環境**:
```bash
# PostgreSQL 主資料庫
DATABASE_URL="postgresql://aiva:password@localhost:5432/aiva"

# Chroma 向量資料庫
VECTOR_DB_PATH="/data/aiva/chroma"
VECTOR_DB_COLLECTION="aiva_knowledge"

# InfluxDB 時序資料庫（可選）
INFLUX_URL="http://localhost:8086"
INFLUX_TOKEN="your_token"
```

**部署位置**: `services/integration/`
- `services/integration/aiva_integration/reception/` - ExperienceDB
- `services/integration/aiva_integration/knowledge/` - KnowledgeDB
- `services/integration/aiva_integration/execution/` - TraceDB
- `services/integration/capability/` - CapabilityDB

**預估容量**: 
- 初始: 500MB
- 6 個月後: 1-2GB
- 1 年後: 2-3GB

---

### ⏱️ 開發時程

**總計: 8 週完成**

| 階段 | 時間 | 優先級 | 目標 |
|------|------|--------|------|
| 第一階段 | Week 1-2 | 🔴 極高 | 訓練功能 |
| 第二階段 | Week 3-4 | 🟠 高 | RAG 整合 |
| 第三階段 | Week 5-6 | 🟡 中 | 記憶機制 |
| 第四階段 | Week 7-8 | 🟢 中低 | 完整架構 |

---

### 📊 效能預估

| 指標 | 預估值 | 備註 |
|------|--------|------|
| 推理延遲 | 1.5-2.5 ms | 單次決策 |
| 訓練時間 | 2-5 分鐘/epoch | 1000 樣本 |
| 準確率 | 70-85% | 訓練後 |
| 記憶體 | 150-200 MB | 含資料庫快取 |
| 磁碟空間 | 500MB → 3GB | 1 年增長 |

---

### 🎓 關鍵成功因素

#### 1. 優先實現訓練功能 🔴

**原因**: 
- 當前最嚴重的問題是無法學習
- 隨機權重導致準確率僅 5%
- 訓練功能是其他功能的基礎

**行動**:
- 立即開始第一階段開發
- 2 週內完成基礎訓練循環
- 驗證準確率提升到 40%+

#### 2. 建立高質量訓練數據集 📊

**原因**:
- 數據質量直接影響模型效果
- 需要足夠的成功/失敗案例
- 需要多樣化的攻擊場景

**行動**:
- 收集真實攻擊場景數據
- 建立數據標註流程
- 目標: 初始 1000+ 樣本

#### 3. 逐步驗證各模組功能 ✅

**原因**:
- 避免一次改動太多導致難以調試
- 每個階段都有明確的驗證標準
- 保證質量的同時穩步前進

**行動**:
- 每個階段完成後進行全面測試
- 使用 CI/CD 自動化測試
- 保持測試覆蓋率 > 80%

#### 4. 保持與現有架構的兼容性 🔄

**原因**:
- AIVA 已有完整的五大模組架構
- 避免破壞性變更影響其他模組
- 確保平滑升級

**行動**:
- 使用適配器模式保持介面一致
- 提供向後兼容的 API
- 充分的集成測試

---

## 🚀 立即行動建議

### 第一步: 建立開發環境

```bash
# 1. 安裝依賴
pip install torch numpy sqlalchemy chromadb

# 2. 初始化資料庫
python scripts/init_databases.py

# 3. 驗證當前模型
python -c "
from services.core.aiva_core.ai_engine.bio_neuron_core import BioNeuronCore
core = BioNeuronCore()
status = core.get_system_status()
print(status)
"
```

### 第二步: 實現訓練功能

```bash
# 創建訓練模組
mkdir -p services/core/aiva_core/ai_engine/training/

# 實現核心文件:
# - training/trainer.py (訓練器)
# - training/data_collector.py (數據收集器)
# - training/loss_functions.py (損失函數)
```

### 第三步: 收集訓練數據

```bash
# 啟動數據收集
python scripts/collect_training_data.py --target http://testphp.vulnweb.com

# 驗證數據質量
python scripts/validate_dataset.py --dataset-id 1
```

### 第四步: 開始訓練

```bash
# 第一次訓練
python -m services.core.aiva_core.ai_engine.training.trainer \
    --dataset training_v1 \
    --epochs 100 \
    --batch-size 32 \
    --learning-rate 0.001

# 驗證準確率
python -m services.core.aiva_core.ai_engine.training.evaluator \
    --model-version v1.0 \
    --test-dataset test_v1
```

---

## 📝 附錄

### A. 參數計算公式

```
全連接層參數 = input_size × output_size + output_size (bias)

LSTM 參數 = 4 × (input_size × hidden_size + hidden_size × hidden_size + hidden_size)
  - 4 個門: 輸入門、遺忘門、輸出門、候選記憶

多頭注意力參數 = 3 × (d_model × d_model) + d_model
  - Q, K, V 映射 + 輸出投影

BiologicalSpiking = input_size × output_size
  - 簡化模型，無額外參數
```

### B. 記憶體計算

```
float32 參數記憶體 = 參數數量 × 4 bytes

訓練記憶體 ≈ 模型記憶體 × 3
  - 模型參數 × 1
  - 梯度 × 1
  - 優化器狀態 × 1 (Adam)

推理記憶體 ≈ 模型記憶體 × 1.5
  - 模型參數 × 1
  - 中間激活 × 0.5
```

### C. 相關代碼位置

```
核心 AI:
- services/core/aiva_core/ai_engine/bio_neuron_core.py
- services/core/aiva_core/ai_commander.py

資料庫:
- services/integration/aiva_integration/reception/experience_repository.py
- services/integration/aiva_integration/reception/experience_models.py

執行追蹤:
- services/core/aiva_core/execution_tracer/
- services/core/aiva_core/analysis/ast_trace_comparator.py

工具註冊:
- services/integration/capability/
```

### D. 參考文獻

- AIVA AI Enhancement Report (AIVA_AI_ENHANCEMENT_COMPREHENSIVE_IMPROVEMENT_REPORT.md)
- AIVA 22 AI Components Guide (reports/ai_analysis/AIVA_22_AI_COMPONENTS_DETAILED_GUIDE.md)
- AIVA Realistic Capability Assessment (AIVA_REALISTIC_CAPABILITY_ASSESSMENT.md)
- BioNeuron RAG Agent Implementation (bio_neuron_core.py)

---

## 📧 聯絡資訊

如有任何問題或建議，請聯繫開發團隊。

---

**報告生成時間**: 2025年11月9日  
**版本**: v1.0  
**狀態**: 待實施

╔═══════════════════════════════════════════════════════════════╗
║  建議: 立即開始實施方案 C 的第一階段（添加訓練功能）         ║
║  預計 2 週內完成，準確率可從 5% 提升到 40%+                  ║
╚═══════════════════════════════════════════════════════════════╝
